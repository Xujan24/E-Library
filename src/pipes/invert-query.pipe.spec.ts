import { InvertQueryPipe } from './invert-query.pipe';

describe('InvertQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new InvertQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
